name: B2

on:
  schedule:
    - cron: '0 */1 * * *' # يشغل الكود كل ساعة
  push: # عند كل عملية دفع جديدة
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config user.name "gasem_hany" # استبدل باسم المستخدم الفعلي
          git config user.email "gasemar684@gmial.com" # استبدل بالبريد الإلكتروني الفعلي
          git remote set-url origin https://${{ secrets.B2}}@github.com/gasemhany/B2.git # استبدل بالقيم الصحيحة

      - name: Run B2 Tasks
        run: |
          echo "Executing simple auto-refresh tasks..."
          git pull
          git push
